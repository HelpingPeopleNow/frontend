node_modules/
dist/
coverage/
playwright-report/
test-results/

test-results/
playwright-report/

# Environment files — the live env file is `.env` (never tracked); only `*.example` templates are tracked
.env
.env.*
*.env
secrets/
!.env.example
!.env.*.example
